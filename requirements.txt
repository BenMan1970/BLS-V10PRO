streamlit>=1.35,<2.0
pydantic>=2.7,<3.0
jinja2>=3.1,<4.0

# ─────────────────────────────────────────────────────────────────────────────
# weasyprint volontairement DÉSACTIVÉ sur Streamlit Community Cloud.
# Il exige libpango/libcairo/libharfbuzz (apt), or l'installation apt de
# l'image Cloud est cassée (dépôt bullseye-security expiré -> exit code != 0).
# ENGINE.V10.py gère son absence (_HAS_WEASYPRINT = False) : le rapport HTML
# est calibré A4 et s'imprime en PDF depuis le navigateur.
# Pour un déploiement Docker / VPS / HF Spaces, décommenter la ligne suivante
# ET réactiver packages.txt :
# weasyprint>=61,<63
# ─────────────────────────────────────────────────────────────────────────────
